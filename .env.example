# Throughline environment variables — copy to .env and fill in real values.
# Secrets via env only. Never commit the real .env (it is gitignored).

# Anthropic (BYO-LLM for Hume EVI 3 / Companion runtime)
export ANTHROPIC_API_KEY=""
export ANTHROPIC_MODEL="claude-sonnet-4-6"

# Notion (specs / source of truth)
export NOTION_TOKEN=""

# Hume EVI 3 (live two-way voice, ZDR)
export HUME_API_KEY=""
export HUME_SECRET_KEY=""
export HUME_CONFIG_ID=""
export HUME_SETH_VOICE_ID=""

# Supabase (application data — single source of truth)
export SUPABASE_URL="https://uuzzfeaevxilwizaittq.supabase.co"
export SUPABASE_ANON_KEY=""
export SUPABASE_SERVICE_ROLE_KEY=""

# Feature flags
export FEATURE_VOICE_SETH=""

# OpenAI
export OPENAI_API_KEY=""

# --- Client-exposed (apps/web). Vite inlines VITE_* into the browser bundle.
# Put ONLY client-safe values here (Supabase project URL + anon/public key).
# NEVER give a server secret a VITE_ prefix. See CLAUDE.md rule #6.
export VITE_SUPABASE_URL="https://uuzzfeaevxilwizaittq.supabase.co"
export VITE_SUPABASE_ANON_KEY=""
# Feature flags (client) — opt-in, set to "true" to enable. Off by default.
export VITE_FEATURE_FIRST_THREAD_VOICE="false"
